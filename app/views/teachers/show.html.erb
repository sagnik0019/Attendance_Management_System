<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="card">
        <div class="card-header bg-primary text-white">
          <div class="d-flex justify-content-between align-items-center">
            <h2 class="mb-0">Teacher Details</h2>
            <div>
              <%= link_to 'Back', teachers_path, class: 'btn btn-sm btn-outline-light me-2' %>
              <%= link_to 'Edit', edit_teacher_path(@teacher), class: 'btn btn-sm btn-outline-light' %>
            </div>
          </div>
        </div>

        <div class="card-body">
          <div class="row mb-3">
            <div class="col-md-4 fw-bold">Name:</div>
            <div class="col-md-8"><%= @teacher.name %></div>
          </div>

          <div class="row mb-3">
            <div class="col-md-4 fw-bold">Email:</div>
            <div class="col-md-8"><%= @teacher.email %></div>
          </div>



          <div class="row mb-3">
            <div class="col-md-4 fw-bold">Created At:</div>
            <div class="col-md-8"><%= @teacher.created_at.to_formatted_s(:long) %></div>
          </div>

          <div class="row">
            <div class="col-md-4 fw-bold">Updated At:</div>
            <div class="col-md-8"><%= @teacher.updated_at.to_formatted_s(:long) %></div>
          </div>
        </div>

        <div class="card-footer bg-light">
          <div class="d-flex justify-content-end">
            <%= button_to 'Delete', @teacher,
                  method: :delete,
                  data: { confirm: 'Are you sure you want to delete this teacher?' },
                  class: 'btn btn-danger' %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
