<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Semesters</h1>
    <%= link_to "Add Semester", new_semester_path, class: "btn btn-primary" %>
  </div>

  <div class="table-responsive">
    <table class="table table-striped table-hover">
      <thead class="table-dark">
        <tr>
          <th>Name</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @semesters.each do |semester| %>
          <tr>
            <td><%= semester.name %></td>
            <td>
              <div class="btn-group" role="group">
                <%= button_to "Show", semester, class: "btn btn-sm btn-outline-primary me-1" %>
                <%= button_to "Edit", edit_semester_path(semester), class: "btn btn-sm btn-outline-secondary me-1" %>
                <%= button_to "Destroy", semester, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-sm btn-outline-danger me-1" %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
